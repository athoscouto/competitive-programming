#include <bits/stdc++.h>

using namespace std;

int main() {
    int n, m;

    scanf("%d %d ", &n, &m);

    char p[1000000];
    fgets(p);
    int len = strlen(p);

    int old, news;
    scanf("%d", &old);
    for (int i = 0; i < m; i++) {
        scanf("%d", &news);
        if (news - old)

    return 0;
}
